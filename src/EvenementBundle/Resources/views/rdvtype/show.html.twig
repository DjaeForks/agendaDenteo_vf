{% extends 'base.html.twig' %}

{% block body %}
    <h1>RdvType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rdvType.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rdvType.titre }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ rdvType.duree }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ rdvType.couleur }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rdvType.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rdvtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rdvtype_edit', { 'id': rdvType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
